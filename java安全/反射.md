## 概念
反射是⼤多数语⾔⾥都必不可少的组成部分，对象可以通过反射获取他的类，类可以通过反射拿到所有⽅法（包括私有），拿到的⽅法可以调⽤，总之通过“反射”，我们可以将Java这种静态语⾔附加上动态特性。
java反射机制可以无视类方法，变量去访问权限修饰符，并且可以调用任何类的任意方法，访问并修改成员变量值。通俗的说java反射可以在运行时根据传参确定要操作的类是什么，并且可以在运行时获取类的完整构造，并调用对应的方法。反射提供了一种动态创建对象的方式。
**反射机制允许程序在执行期借助于ReflectionAPI取得任何类的内部信息（比如成员变量、构造器、成员方法等等），并能操作对象的属性及方法。加载完类后，在堆中就产生了一个Class类型的对象（一个类只有一个Class对象），这个对象包含了类的完整结构信息。通过这个对象得到类的结构。**
#### java反射机制原理图
![](image/356.png)
设想一个场景，如果需要根据配置文件re.properties指定信息，创建对象并调用方法。
```
classfullpath=com.lxflxf.Cat
method=hi
```
这样的需求在框架时很多，通过外部文件控制，在不修改源码的情况下，来控制程序。
实践一下，先创建配置文件，写入上述内容，然后创建一个类，写入如下内容
```java
public class Cat {  
    private String name = "小猫";  
    public void hi() {  
        System.out.println("hi" + name);  
    }  
    public static void main(String[] args) {  
        Cat cat = new Cat();  
        cat.hi();  
    }  
}
```
传统方法是要new一个对象，然后再调用他的方法。
```java
Cat cat = new Cat(); 
cat.hi();
```
这样确实可以调用hi()方法，但是我们要根据配置文件re.properties指定信息来完成，可以试一下用IO流来读取配置文件信息，
```java
Properties properties = new Properties();  
// 2. 声明流对象（try-with-resources 自动关闭资源）  
try (FileInputStream fis = new FileInputStream("src/re.properties")) {  
    // 3. 加载配置文件  
    properties.load(fis);  
    String classfullpath = properties.getProperty("classfullpath");  
    String methodName = properties.getProperty("method");  
    System.out.println("classfullpath = " + classfullpath);  
    System.out.println("methodName = " + methodName);
```
运行一下发现可以读取到内容，但是如何创建对象，不可以直接`new classfullpath`,因为`classfullpath`是字符串类型，不可以去new，到这里就要用到反射了。
#### 例子
第一步，加载类，返回Class类型的对象cls
```java
Class cls = Class.forName(classfullpath);
```
第二步，通过cls得到加载的类com.lxflxf.Cat 的对象实例
```java
Object o = cls.newInstance();
```
可以打印看一下拿到的是不是`com.lxflxf.Cat`
```java
System.out.println(o.getClass());
```
第三步，通过cls得到你加载的类com.lxflxf.Cat 的methodName方法对象。我们可以在反射中把方法视为对象
```java
Method method1 = cls.getMethod(methodName);
```
最后通过method1来调用方法，也就是通过方法对象来调用方法。
```java
method1.invoke(o);
```
完整代码
```java
  
import java.io.FileReader;  
import java.lang.reflect.Method;  
import java.util.Properties;  
  
public class TestReflect {  
    public static void main(String[] args) throws Exception {  
        // 读取配置文件，获取类全路径和方法名  
        Properties properties = new Properties();  
        // 加载配置文件  
        properties.load(new FileReader("src//re.properties"));  
        String classfullpath = properties.getProperty("classfullpath");  
        String methodName = properties.getProperty("method");  
        Class cls = Class.forName(classfullpath);  
        Object o = cls.newInstance();  
        System.out.println(o.getClass());  
        Method method1 = cls.getMethod(methodName);  
        method1.invoke(o);  
    }  
}
```
这里也可以看出反射和传统方法的区别了，传统是`对象.方法()`而反射是`方法.invoke(对象)`
反射还有一个很大的优点就是可以通过外部文件控制，不修改源码，只修改外部配置文件来控制程序。
例如可以在Cat类下新加一个方法cry
```java
public  void cry() {  
    System.out.println(name + " ~~~~喵喵喵");  
}
```
然后修改配置文件`re.properties`将method=hi改为method=cry，就可以了。
再运行就会输出`小猫 ~~~~喵喵喵`
## 反射的作用及具体运用
在运行时判断一个对象所属的类。
在运行时构造一个类的对象。
在运行时判断一个类所具有的成员变量和方法。
在运行时调用任意一个对象的方法。
生成动态代理。
在开发过程中使用Eclipse、IDEA等开发工具时，当我们输入一个对象或类并想调用它的属性或方法时，编译器会自动列出它的属性或方法
JavaBean和JSP之间的调用也是通过反射实现的
反射最重要的用途是开发各种通用框架，例如Spring，ORM等。
**利用反射执行系统指令的例子**
```java
import java.lang.reflect.Method;  
  
public class Main {  
    public static void main(String[] args) throws Exception {  
        //通过反射获取Runtime类  
        Class<?> cls = Class.forName("java.lang.Runtime");  
        //使用getMethod方法获取getRuntime方法  
        Method getRuntime1 = cls.getMethod("getRuntime");  
        //使用invoke方法静态调用getRuntime方法  
        Object o = getRuntime1.invoke(null);  
        Method exec1 = cls.getMethod("exec", String.class); // 获取exec(String)方法  
        exec1.invoke(o,"calc.exe");  //等价于o.exec("calc.exe")
    }  
}
```
运行就相当于执行了系统指令`calc.exe`，打开了计算器。
如果改成`whoami`，发现它没有办法输出结果。这是因为calc.exe是GUI程序，不需要窗口输出。而whoami的执行结果输出到标准输出流（stdout）。
要想要输出结果，需要读取stdout并打印它。
当系统执行命令时，系统会给他三个流
```
stdout(标准输出流) stdin(标准输入流) stderr(标准错误流)
```
java可以通过
```java
process.getInputStream()   // 读取 stdout
process.getErrorStream()   // 读取 stderr
process.getOutputStream()  // 向 stdin 写入
```
如果代码改成这样就可以得到执行结果了
```java
import java.io.BufferedReader;  
import java.io.InputStreamReader;  
import java.lang.reflect.Method;  
  
public class Main {  
    public static void main(String[] args) throws Exception {  
        //通过反射获取Runtime类  
        Class<?> cls = Class.forName("java.lang.Runtime");  
        //使用getMethod方法获取getRuntime方法  
        Method getRuntime1 = cls.getMethod("getRuntime");  
        //使用invoke方法动态调用getRuntime方法  
        Object o = getRuntime1.invoke(null);  
        Method exec1 = cls.getMethod("exec", String.class); // 获取exec(String)方法  
        //JVM创建一个操作系统子进程 ，执行whoami,返回一个process对象  
        Process process = (Process) exec1.invoke(o, "whoami");  
        BufferedReader reader = new BufferedReader(new InputStreamReader(process.getInputStream()));  
        String line;//声明一个String类型的变量line  
        while ((line = reader.readLine())!=null){ //while循环一行一行读取，直到没有数据为止  
            System.out.println(line);  
        }  
    }  
}
```
其中最重要的是`BufferedReader reader = new BufferedReader(new InputStreamReader(process.getInputStream()));`
- `process.getInputStream()`获取外部进程process的标准输出流
- `new InputStreamReader(...)`将底层的字节流（InputStream）转换为字符流（reader）。
- `new BufferedReader(...)`对字符流进行缓冲包装，提升读取效率，同时提供按行读取的便捷方法。
这样就可以执行whoami得到执行结果了。但是有一个要注意的点，就是执行`whoami`和`ipconfig`这样的指令时可以直接写，那是因为它们都有对应的exe可执行文件，而`dir`和`echo`没有对应的可执行文件，正确写法是
```
cmd /c echo %PATH%
cmd /c dir
```
这样就都可以执行了。


## Class类
Class也是一个java类，保存的是与之对应java类的meta信息，用来描述这个类有哪些成员，有哪些方法等。

Java 源程序（.java 文件）在经过 Java 编译器编译之后就被转换成 Java 字节代码（.class 文件）。类加载器负责读取 Java 字节代码，并转换成 java.lang.Class类的一个实例（Class 对象）。也就是说，在 Java 中，每个 java 类都有一个相应的 Class 对象，用于表示这个 java 类的类型信息。
**反射的主要类如下**
```
java.lang.Class 代表一个类，Class对象表示某个类加载后在堆中的对象
java.lang.reflect.Method 代表类的方法
java.lang.reflect.Field 代表类的成员变量
java.lang.reflect.Constructor:代表类的构造方法
```
## 哪些类型有class对象
- 外部类，成员内部类，静态内部类，局部内部类，匿名内部类。
- interface：接口。
- 数组。
- enum：枚举。
- annotation：注解.
- 基本数据类型。
- void。
案例代码
```java
//获取String外部类的Class对象
Class<String> cls1 = String.class; //外部类
//获取 Serializable 接口的Class对象 
Class<Serializable> cls2 = Serializable.class; // 接口类  
//获取 Integer数组的Class对象
Class<Integer[]> cls3 = Integer[].class; // 数组  
//获取 Deprecated注解的Class对象
Class<Deprecated> cls4 = Deprecated.class; //注解  
System.out.println(cls1);  
System.out.println(cls2);  
System.out.println(cls3);  
System.out.println(cls4); 
```
输出
```
class java.lang.String  
interface java.io.Serializable  
class [Ljava.lang.Integer;  
interface java.lang.Deprecated  
```
## 反射要用到的一些方法
例子代码
```java
package com.lxflxf;  
  
public class Car {  
    public String brand;  
    public int prise;  
    public String color;  
  
    public static void main(String[] args) throws  Exception{  
        //获取到Car类的Class对象  
        Class cls = Class.forName("com.lxflxf.Car");  
        System.out.println(cls);  
        System.out.println(cls.getClass());  
    }  
}
```
输出
```
class com.lxflxf.Car
class java.lang.Class
```
第一个输出的是cls对象，是哪个类的Class对象，第二个输出的是Class的运行类型。
#### 获得名为Class的类的对象
- 使用`forName()`方法：`forName()`接受字符串参数（类的名称）并返回Class对象，返回的对象引用字符串指定的类。
```java
Class cls = Class.forName("com.lxflxf.Car");//获得Class对象
```
- 使用`getClass()`方法：使用特定类的对象来创建新的Class对象。
```java
Car cr = new Car();  
Class cls = cr.getClass();
```
- 使用`.class`：java 的每个类中默认会创建一个名为 `class` 的属性，该属性就是该类的 `Class` 对象。我们还可以使用`.class`扩展名创建Class对象。
```java
Class cls = Car.class; //使用 类名.class 来获得Class对象
```
创建Class对象后就可以使用这些对象执行反射。
```java
System.out.println(cls.getClass()); //输出Class对象的类型 例如 class java.lang.Class
System.out.println(cls.getPackageName()); //输出包名 例如 com.lxflxf
System.out.println(cls.getName()); //输出类名，例如 com.lxflxf.Car
```
#### 创建对象实例
- 使用`newInstance()`，例如
```java
Object o = cls.newInstance();
```
这里也需要注意一点，在JDK1.9往上，不再使用newInstance()。
#### 获取接口
若一个类实现了一个接口，可以使用Class的`getInterfaces()`方法来获取，此方法返回一个接口数组。
```java
Class obj = d1.getClass(); // 获得Class对象
Class[] objInterface = obj.getInterface(); // 获得接口
```
#### 获取父类和访问修饰符
类Class的方法`getSuperclass()`可用于获取有关特定类的父类的信息。
而且`Class`提供了一种`getModifier()`方法，该方法以整数形式返回class的修饰符。
```java
Class cls = Car.class;//获取到Car类的Class对象
int modifier = cls.getModifiers();  //以整数形式获取Car的访问修饰符 ，这里输出1
Class superclass = cls.getSuperclass();  //获取父类
System.out.println(superclass);//输出 class java.lang.Object
```
#### 获取字段(属性)
**我们可以使用 `Field` 类提供的各种方法检查和修改类的不同字段。**
- `getFields()`返回该类及其父类的所有公共字段。
```java
Class cls = Car.class;
Field[] field1 = cls.getDeclaredFields();
Field[] field2 = cls.getFields();
System.out.println(cls.getFields()); //输出 [Ljava.lang.reflect.Field;@6acbcfc0
System.out.println(Arrays.toString(field1)); // 输出 [public java.lang.String com.lxflxf.Car.brand, public int com.lxflxf.Car.prise, public java.lang.String com.lxflxf.Car.color] 这样就获取到了属性
System.out.println(Arrays.toString(field2));//输出同上
System.out.println(cls.getField("color")); // 输出 public java.lang.String com.lxflxf.Car.color
```
- **getModifier() -> int** 以整数形式返回字段的修饰符
- **set(Object, value) -> void** 使用指定的值设置字段的值（注意是 Object，是个对象实列）
- **get(Object) -> Object** 获取字段的值 （注意是 Object）
- **setAccessible(boolean) -> void** 使私有字段可访问
- **getField(“fieldName”) -> Field** 从类返回名称为 **fieldName** 的公共字段。
- **getDeclaredField(“fieldName”) -> Field** 从类返回名称为 **fieldName** 的字段。（若获得的是私有字段，则要在使用之前设置 `setAccessible(true)`）
例如上面的例子代码
```java
Car cr = new Car();  
//创建Class对象
Class cls = cr.getClass();  
//获取Car类的brand属性
Field field1 = cls.getField("brand");
//设置属性值为ben  
field1.set(cr,"ben");  
//拿到brand的属性值并输出
System.out.println(field1.get(cr));
//获取类的访问修饰符  
int mod1 = field1.getModifiers();  
System.out.println(mod1);
```
代码执行效果
```
ben
1
```
#### 访问私有字段
上面的例子里属性都是`public`，如果属性是私有字段怎么访问？
设有
```java
public class Car {  
    public String brand;  
    public int prise = 50000;  
    private String color;
}
```

```java
Car cr = new Car();  
Class cls = cr.getClass();  
//访问私有属性
Field field2 = cls.getDeclaredField("color");
//使私有字段可以访问  
field2.setAccessible(true);  
//设置color值（第一个参数为对象实例）
field2.set(cr,"black");  
System.out.println(cr.color);  
//获取类的访问修饰符
int mod2 = field2.getModifiers();  
System.out.println(mod2);
```
执行结果
```
black
2
```
#### 获取方法
和属性一样，我们可以使用`Method`类提供的各种方法来检测类的不同方法。
- **getMethods()** -> `Method[]` 返回该类及其超类的所有公共方法
- **getDeclaredMethod()** -> `Method[]` 返回该类的所有方法
- **getName()** -> `String` 返回方法的名称
- **getModifiers()** -> int 以整数形式返回方法的访问修饰符
- **getReturnType()** -> `Class<?>` 返回方法的返回类型

```java
package com.lxflxf;  
import java.lang.reflect.Method;  
  
  
public class Car {  
    public void hello(){  
        System.out.println("hello I'm a Car!");  
    }  
    protected void drink(){  
        System.out.println("I drink oil.");  
    }  
    private void Sound(){  
        System.out.println("I'm a car,I can make a sound.");  
    }  
}  
class ReflectionDemo {  
    public static void main(String[] args) {  
        try {  
            Car cr = new Car();  
            Class cls = cr.getClass();  
            Method[] methods = cls.getDeclaredMethods();  
            System.out.println(Arrays.toString(methods));
            for(Method m: methods){  
                //获取方法名称  
                System.out.println("方法名称： "+m.getName());  
                int modifiers = m.getModifiers();  
                //获取方法的访问修饰符  
                System.out.println("修饰符： "+Modifier.toString(modifiers));  
                //获取返回值类型  
                System.out.println("返回值类型： "+m.getReturnType());  
  
            }  
        } catch (Exception e) {  
            e.printStackTrace();  
        }  
    }  
}
```
运行后得到结果
```
[private void com.lxflxf.Car.Sound(), protected void com.lxflxf.Car.drink(), public void com.lxflxf.Car.hello()]
方法名称： Sound
修饰符： private
返回值类型： void
方法名称： drink
修饰符： protected
返回值类型： void
方法名称： hello
修饰符： public
返回值类型： void
```
如果写成`System.out.println("修饰符： "+modifiers);`修饰符就会输出成对应的数字。
#### 获取构造方法
**可以使用 `Constructor` 类提供的各种方法检查类的不同构造函数。**
- **getConstructors()**  返回该类的所有公共构造函数以及该类的父类
- **getDeclaredConstructor()** -返回所有构造函数
- **getName()** - 返回构造函数的名称
- **getModifiers()** - 以整数形式返回构造函数的访问修饰符
- **getParameterCount()** - 返回构造函数的参数数量
```java
package com.lxflxf;  
import java.lang.reflect.Constructor;  
import java.lang.reflect.Modifier;  
import java.util.Arrays;  
  
  
class Dog {  
    public Dog() {  
    }  
    public Dog(int age) {  
    }  
    private Dog(String sound, String type) {  
    }  
}  
  
class ReflectionDemo {  
    public static void main(String[] args) {  
        try {  
            Dog d1 = new Dog();  
            Class obj = d1.getClass();  
            //使用getDeclaredConstructor()获取一个类中的所有构造函数  
            Constructor[] constructors = obj.getDeclaredConstructors();  
            System.out.println(Arrays.toString(constructors));  
            for(Constructor c : constructors) {  
                //获取构造函数的名称  
                System.out.println("构造函数名称： " + c.getName());  
                //获取构造函数的访问修饰符  
                int modifier = c.getModifiers();  
                System.out.println("修饰符： " + Modifier.toString(modifier));  
                //获取构造函数中的参数数量  
                System.out.println("参数数量： " + c.getParameterCount());  
            }  
        }  
        catch(Exception e) {  
            e.printStackTrace();  
        }  
    }  
}
```
运行后得到的结果
```
[public com.lxflxf.Dog(), public com.lxflxf.Dog(int), private com.lxflxf.Dog(java.lang.String,java.lang.String)]
构造函数名称： com.lxflxf.Dog
修饰符： public
参数数量： 0
构造函数名称： com.lxflxf.Dog
修饰符： public
参数数量： 1
构造函数名称： com.lxflxf.Dog
修饰符： private
参数数量： 2
```
#### 实例化对象与执行函数
- `newInstance()`：实例化类对象的方法，位于Class类中（只能无参构造）。
```java
class Car {  
    public void display(){  
        System.out.println("I am a dog.");  
    }  
    public static void main(String[] args) throws Exception {  
        Class clsd = Class.forName("com.lxflxf.Car");  
        Car car = (Car) clsd.newInstance();  
        car.display();  
    }  
}
```
- `invoke()`执行函数方法，位于Method类中。
```java
public class Cat {  
    public static void main(String[] args) throws  Exception {  
        execute("com.lxflxf.Car","display");  //调用函数
    }  
    //自定义反射函数execute
    public static void execute(String className , String methodName) throws Exception{  
        Class cls = Class.forName(className);  
        Object o = cls.newInstance();  
        cls.getMethod(methodName).invoke(o);  
    }  
}
```
这样就可以执行`com.lxflxf.Car`类中的`display`函数。
## 重点方法
- 获取类的方法 `foName`
- 实例化类对象的方法 `newInstance`
- 获取函数的方法 `getMethod`
- 执行函数的方法 `invoke`
基本上，这几个方包揽了Java安全里各种和反射有关的Payload。
```java
public void execute(String className, String methodName) throws Exception {
     Class clazz = Class.forName(className);
     clazz.getMethod(methodName).invoke(clazz.newInstance());
}
```
## 动态加载
通过“反射”，我们可以将Java这种静态语⾔附加上动态特性，换句话说，就是反射机制是Java实现动态语言的关键，也就是通过反射实现类动态加载。就是在运行时加载需要的类，如果运行时不用该类，则不报错，降低了依赖性。
例如
```java
import java.lang.reflect.Method;  
import java.util.Scanner;  
  
public class ClassLoad {  
    public static void main(String[] args) throws Exception {  
    //获取用户输入
        Scanner scanner = new Scanner(System.in);  
        System.out.println("请输入数字：");  
        String key = scanner.next();  
        switch (key){  
            case "1":  
                System.out.println("我是数字1");  
                break;  
            case "2":  
                Class<?> cls = Class.forName("person");  
                Object o = cls.newInstance();  
                Method method = cls.getMethod("show");  
                method.invoke(o);  
                System.out.println("ok");  
                break;  
        }  
    }  
}
```
这里是没有person类的，但是程序编译时不会报错，也就是当程序执行到`case "2":`才会发生报错，也就是**在运行时加载需要的类，如果运行时不用该类，则不报错，这就是动态加载。**
输入1的话，程序正常
```
请输入数字：
1
我是数字1
```
输入2的话，程序报错
```
请输入数字：
2
Exception in thread "main" java.lang.ClassNotFoundException: person
	at java.base/jdk.internal.loader.BuiltinClassLoader.loadClass(BuiltinClassLoader.java:641)
	at java.base/jdk.internal.loader.ClassLoaders$AppClassLoader.loadClass(ClassLoaders.java:188)
	at java.base/java.lang.ClassLoader.loadClass(ClassLoader.java:526)
	at java.base/java.lang.Class.forName0(Native Method)
	at java.base/java.lang.Class.forName(Class.java:421)
	at java.base/java.lang.Class.forName(Class.java:412)
	at ClassLoad.main(ClassLoad.java:15)
```
里面有一句`java.lang.ClassNotFoundException: person`，没有找到person类。
这就是动态加载。

## 类加载
![](image/357.png)

## 类的初始化
`forName()`有两个函数函数重载，
- `class<?> forName(String name);`
- `class<?> forName(String name, **boolean** initialize, ClassLoader loader);` 
第一个就是常见的获取`Class`的方式，可以理解为第二个方式的一种封装。
```java
class<?> forName(String name);
//等于
Class.forName(className, true, currentLoader)
```
例子
创建一个类
```java
public class TrainPrint {  
    {  
        System.out.printf("Empty block initial %s\n",this.getClass());  
    }  
    static {  
        System.out.printf("Static initial %s\n",TrainPrint.class);  
    }  
    public TrainPrint(){  
        System.out.printf("Initial %s\n", this.getClass());  
    }  
}
```
再执行
```java
public class Main {  
    public static void main(String[] args) {  
        new TrainPrint();  
    }  
}
```
根据执行结果可以看到调用顺序
```
Static initial class TrainPrint
Empty block initial class TrainPrint
Initial class TrainPrint
```
可以看到首先调用的是`static{}`，其次是`{}`，最后是构造函数。
其中`static{}`就是在类初始化的时候调用的，而`{}`中的代码会放在构造函数的`super()`后面，但在当前构造函数的前面。
所以`forName()`中的`initialize=true`其实就是告诉java虚拟机是否执行‘类初始化’。
## P神文章照抄
#### 在正常情况下，除了系统类，如果我们想要拿到一个类，需要先`import`后才可以使用，而使用`forName`就不需要，这样就可以加载任意类。
java的普通类hello支持编写内部类，`hello2`，而在编译的时候，会生成两个文件，`hello.class`和`hello$hello2.class`，我们可以把他看作两个无关的类，通过`Class.forName("hello$hello2")`即可加载这个内部类。`$`的作用是查找内部类，获得类以后，我们可以继续使用反射来获取这个类中的属性方法，也可以实例化这个类，并调用方法。
`class.newInstance()`的作用就是调用这个类的无参构造函数，如果不成功可能是因为使用的类里面没有无参构造函数，或者使用的类构造函数是私有的。
在构造payload时最常见的类就是`java.lang.Runtime`
如果我们直接这样执行命令
```java
Class clazz = Class.forName("java.lang.Runtime");  
clazz.getMethod("exec", String.class).invoke(clazz.newInstance(),"id");
```
会发现报错里说
```
class Main cannot access a member of class java.lang.Runtime (in module java.base) with modifiers "private"
```
说明Runtime类是私有的。
之所以有的类的构造方法是私有的，这就涉及到单例模式，简单来说，例如，对于Web应用来说，数据库连接只需要建立一次，而不是每次用到数据库的时候再新建立一个连接，此时作为开发者你就可以将数据库连接使用的类的构造函数设置为私有，然后编写一个静态方法来 获取：
```java
public class TrainDB {
	private static TrainDB instance = new TrainDB();
	public static TrainDB getInstance() {
		return instance;
	}
	private TrainDB() {
		// 建立连接的代码...
	}
}
```
这样只有类初始化时会执行一次构造函数，后面只有通过`getInstance()`来获得这个对象，避免建立多个数据库连接。
而我们的`Runtime`用的就是单例模式，我们只可以通过`Runtime.getRuntime()`来获取到`Runtime`对象，所以我们只要将上述payload改为这样就可以执行了
```java
Class clazz = Class.forName("java.lang.Runtime");  
clazz.getMethod("exec",String.class).invoke(clazz.getMethod("getRuntime").invoke(clazz),"calc.exe");
```
但是这个还会遇到开头例子遇到的问题(不可以输出执行结果)。用上述方法解决就可以了。
这里用到了`getMethod`和`invoke`方法，
`getMethod`的作用是通过反射获取一个类的某个公有方法，但是java支持方法重载，可以看到exec有6个重载
![](image/358.png)
其中`exec(String commmand)`是最简单的，就使用这个，所以我们使用`getMethod("exec",String.class)`来获取`Runtime.exec`方法，
invoke的作用是执行方法，他的第一个参数是
- 如果这个方法是一个普通方法，那么第一个参数是类对象。
- 如果这个方法是一个静态方法，那么第一个参数是类
正常执行方法是`对象.方法()`，而反射是`方法.invoke(对象)`
如果分解一下payload就是
```java
Class clazz = Class.forName("java.lang.Runtime");  
Method execMethod = clazz.getMethod("exec", String.class);  
Method getRuntimeMethod = clazz.getMethod("getRuntime");  
Object runtime = getRuntimeMethod.invoke(clazz);  
execMethod.invoke(runtime, "calc.exe");
```
#### 但是如果一个类没有无参构造方法，也没有类似单例模式里的静态方法，也可以通过反射实例化改类。
这里需要用到一个新的反射方法`getConstructor`
和`getMethod`类似，`getConstructor`接受的参数是构造函数列表类型，并且构造函数也支持重载,所以必须用参数列表类型才可以唯一确定一个构造函数。
获取到构造函数后，可以使用`newInstance`来执行。
比如我们另一种常用的命令执行方式`ProcessBuilder`,我们使用反射来获取其构造函数，然后调用`start()`来执行命令。
例如
```java
Class clazz = Class.forName("java.lang.ProcessBuilder");  
((ProcessBuilder) clazz.getConstructor(List.class).newInstance(Arrays.asList("calc.exe"))).start();
```
这里需要强制类型转换是因为`newInstance()` 的返回类型是 `Object`，而Object 类上没有 start() 方法，所以要转换成`ProcessBuilder`来执行`start()`
ProcessBuilder有两个构造函数
- `public ProcessBuilder(List command)`
- `public ProcessBuilder(String... command)`
上面用到的是第一种的构造函数。但是上面用到了强制类型转换，但是在漏洞利用时在表达式上下文中是没有这种语法的，所以依旧需要用反射来完成这一步
```java
Class clazz = Class.forName("java.lang.ProcessBuilder");  
clazz.getMethod("start").invoke(clazz.getConstructor(List.class).newInstance(Arrays.asList("calc.exe")));
```
这里不用强制类型转换是因为`invoke`就是要接受`Object`参数。
但是如果要用`public ProcessBuilder(String... command)`，这个构造函数该怎么执行呢？
因为java是支持可变长参数(varargs)的，就是定义函数时如果不确定参数数量时，可以使用`...`这样的语法来表示参数数量是可变的。
对于可变长参数，Java在编译时会编译成一个数组，也就是说这两种写法在底层是等价的。
```java
public static void main(String...args)
public static void main(String[] args)
```
这样的话就不可以重载了。
也由此，如果我们有一个数组，想传给hello函数，只需直接传即可：
```java
String[] names = {"hello", "world"};  
hello(names);
```
对于反射来说，如果要获取的目标函数里包含可变长参数，只需要认为他是数组就可以了。
所以，我们将字符串数组的类 `String[].class` 传给`getConstructor` ，获取 `ProcessBuilder` 的第二 种构造函数：
在调用 `newInstance` 的时候，因为这个函数本身接收的是一个可变长参数，我们传给 `ProcessBuilder` 的也是一个可变长参数，二者叠加为一个二维数组，所以整个Payload如下：
```java
Class clazz = Class.forName("java.lang.ProcessBuilder");  
clazz.getMethod("start").invoke(clazz.getConstructor(String[].class).newInstance(new String[][]{{"calc.exe"}}));
```
这里之所以要用二维数组，需要这样理解
第一层是`ProcesseBuilder`的构造器签名，
```java
ProcessBuilder(String... command) // 等价于  
ProcessBuilder(String[] command) // 所以要找这个构造器，需要传 String[].class
```

第二层是`newInstance()`传入的是可变参数，`newInstance` 本身也接收可变参数，JVM 会把你传入的东西当作**构造器参数列表**来解包。如果写一维参数的话，JVM 看到一个 `Object[]`，会把它**拆包**，误以为你传了多个 `String` 参数，而不是一个 `String[]` 参数，导致报错。
#### 如果一个方法或构造方法是私有方法，如何执行呢？
这就涉及到`getDeclared`系列的反射了，于普通的`getMethod`，`getConstructor`的区别是
- `getMethod` 系列方法获取的是当前类中所有公共(public)方法，包括从父类继承的方法，不包含私有方法。
- `getDeclaredMethod`系列方法获取的是当前类中'声明'的方法，是实写在这个类里的，包括私有的方法，但从父类里继承的就不包含了。
`getDeclaredMethod` 的具体用法和 `getMethod` 类似， `getDeclaredConstructor` 的具体用法和 `getConstructor` 类似。
举个例子，前文我们说过Runtime这个类的构造函数是私有的，我们需要用 `Runtime.getRuntime()` 来 获取对象。其实现在我们也可以直接用 `getDeclaredConstructor` 来获取这个私有的构造方法来实例 化对象，进而执行命令：
```java
Class clazz = Class.forName("java.lang.Runtime");  
Constructor m = clazz.getDeclaredConstructor();  
m.setAccessible(true);  clazz.getMethod("exec",String.class).invoke(m.newInstance(), "calc.exe");
```
但是不知道为什么，我执行失败了，报错
```java
Unable to make private java.lang.Runtime() accessible: module java.base does not "opens java.lang" to unnamed module @6acbcfc0
```
这个貌似是版本的问题，`Runtime` 的构造器是 `private` 的，在 Java 9+ 中，`setAccessible(true)` 不再万能了。

换成
```java
Class clazz = Class.forName("java.lang.Runtime");   
clazz.getMethod("exec", String.class).invoke(clazz.getMethod("getRuntime").invoke(null), "calc.exe");
```
还是可以。
## 廖雪峰老师的文章
#### class类
除了`int`等基本类型外，java的其他类型全是`class`，包括`interface`，例如
- `String`
- `Object`
- `Runnable`
- `Exception`
- ....
class的本质是数据类型`Type`，无继承关系的数据类型无法赋值。
```java
Number n = new Double(1.0);  
String s = new Double(1.0);
```
第一行正确，第二行报错。因为`Double`是`Number`的子类，二者有继承关系，而`String`和`Double`它们在类树中属于两种不同的分支，没有继承关系，无法进行赋值。
每加载一种`class`，JVM就为其创建一个`Class`类型的实例，并关联起来。注意：这里的`Class`类型是一个名叫`Class`的`class`。它长这样：
```java
public final class Class {
    private Class() {}
}
```
以`String`类为例，当JVM加载`String`类时，它首先读取`String.class`文件到内存，然后，为`String`类创建一个`Class`实例并关联起来：
```java
Class cls = new Class(String);
```
JVM持有的每个`Class`实例都指向一个数据类型（`class`或`interface`）
由于JVM为每个加载的`class`创建了对应的`Class`实例，并在实例中保存了该`class`的所有信息，包括类名、包名、父类、实现的接口、所有方法、字段等，因此，如果获取了某个`Class`实例，我们就可以通过这个`Class`实例获取到该实例对应的`class`的所有信息。
这种通过`Class`实例获取`class`信息的方法称为反射（Reflection）。
有三个方法可以直接获取`class`的`Class`实例
方法一：直接通过一个`class`的静态变量`class`获取：
```java
Class cls = String.class;
```
方法二：如果我们有一个实例变量，可以通过该实例变量提供的`getClass()`方法获取：
```java
String s = "Hello";  
Class cls = s.getClass();
```
方法三：如果知道一个`class`的完整类名，可以通过静态方法`Class.forName()`获取：
```java
Class cls = Class.forName("java.lang.String");
```
这里要注意一点：`Class.forName()` 方法在设计时考虑到：你给的字符串路径（比如 `"java.lang.String"`）可能是错的，或者类根本不存在。因此，它强制要求你处理 `ClassNotFoundException`。
所以必须加上异常处理,可以这样做
```java
public class Main {  
    public static void main(String[] args) throws  Exception {  
        Class cls = Class.forName("java.lang.String");  
        System.out.println(cls);  
    }
```
加上`throws  Exception`
因为反射的目的是为了获得某个实例的信息。因此，当我们拿到某个`Object`实例时，我们可以通过反射获取该`Object`的`class`信息：
```java
void printObjectInfo(Object obj) {
    Class cls = obj.getClass();
}
```
要从`Class`实例获取基本信息，可以参考如下代码
```java
public class Main {  
    public static void main(String[] args) throws  Exception {  
        printClassInfo("".getClass());  
        printClassInfo(Runnable.class);  
        printClassInfo(java.time.Month.class);  
        printClassInfo(int.class);  
        printClassInfo(String[].class);  
    }  
    static void printClassInfo(Class cls){  
        System.out.println("Class name: " + cls.getName());  
        System.out.println("Simple name: " + cls.getSimpleName());  
        if (cls.getPackage() != null) {  
            System.out.println("Package name: " + cls.getPackage().getName());  
        }  
        System.out.println("is interface? " + cls.isInterface());  
        System.out.println("is enum? " + cls.isEnum());  
        System.out.println("is array? " + cls.isArray());  
        System.out.println("is primitive? " + cls.isPrimitive());  
    }  
  
}
```
注意到数组（例如`String[]`）也是一种类，而且不同于`String.class`，它的类名是`[Ljava.lang.String;`。此外，JVM为每一种基本类型如`int`也创建了`Class`实例，通过`int.class`访问。
如果获取到了一个`Class`实例，我们就可以通过该`Class`实例来创建对应类型的实例：
```java
//获取String的Class实例  
Class cls = String.class;  
//创建一个Sting实例  
String s = (String) cls.newInstance();
```
上述代码相当于`new String()`，通过`Class.newInstance()`可以创建类实例，但他的局限是只能调用`public`的无参构造方法。
#### 访问字段
对任意的一个`Object`实例，只要我们获取了它的`Class`，就可以获取它的一切信息。
`Class`类提供了以下几个方法来获取字段：
- `Field getField(name)`：根据字段名获取某个public的field（包括父类）
- `Field getDeclaredField(name)`：根据字段名获取当前类的某个field（不包括父类）
- `Field[] getFields()`：获取所有public的field（包括父类）
- `Field[] getDeclaredFields()`：获取当前类的所有field（不包括父类）
实例
```java
public class Main {  
    public static void main(String[] args) throws  Exception {  
        Class stdClass = Student.class;  
        System.out.println(stdClass.getField("score"));//public int Student.score  
        //System.out.println(stdClass.getField("grade"));//报错，因为getField只能获取public字段  
        System.out.println(stdClass.getField("name"));//public java.lang.String Person.name，可以获得父类字段  
        //System.out.println(stdClass.getDeclaredField("name"));//报错，因为getDeclaredField只能获取本类声明的字段，不可以获取父类或子类字段。  
        System.out.println(stdClass.getDeclaredField("grade"));//private java.lang.String Student.grade ，可以获取私有字段  
    }  
}  
class Person{  
    public String name;  
}  
class Student extends Person{  
    public int score;  
    private String grade;  
}
```
一个`Field`对象包含了一个字段的所有信息：
- `getName()`：返回字段名称，例如，`"name"`；
- `getType()`：返回字段类型，也是一个`Class`实例，例如，`String.class`；
- `getModifiers()`：返回字段的修饰符，它是一个`int`，不同的bit表示不同的含义。
**获取字段值**
例如，对于一个`Person`实例，我们可以先拿到`name`字段对应的`Field`，再获取这个实例的`name`字段的值：
```java
public class Main {  
    public static void main(String[] args) throws  Exception {  
        //创建person实例，设置name属性值  
        Object p = new Person("Xiao Ming");  
        //获取person实例的Class对象  
        Class c = p.getClass();  
        //获取name属性的Field对象  
        Field f = c.getDeclaredField("name");  
        //获取name属性的值  
        Object value = f.get(p);  
        System.out.println(value);  
    }  
}  
class Person{  
    private String name;  
    public Person(String name){  
        this.name = name;  
    }  
}
```
这段代码试图取出`name`属性的值，但是报错了`class Main cannot access a member of class Person with modifiers "private"`没法取出私有属性的值。
只需要在调用`Object value = f.get(p);`前设置`f.setAccessible(true);`
**设置字段值**
设置字段值是通过`Field.set(Object, Object)`实现的，其中第一个`Object`参数是指定的实例，第二个`Object`参数是待修改的值。示例代码如下
```java
public class Main {  
    public static void main(String[] args) throws  Exception {  
        //创建person实例，设置name属性值  
        Object p = new Person("Xiao Ming");  
        //获取person实例的Class对象  
        Class c = p.getClass();  
        //获取name属性的Field对象  
        Field f = c.getDeclaredField("name");  
        //允许访问private属性  
        f.setAccessible(true);  
        //获取name属性的值  
        Object value = f.get(p);  
        System.out.println(value);  
        f.set(p,"Xiao hong");  
        System.out.println(f.get(p));  
    }  
}  
class Person{  
    private String name;  
    public Person(String name){  
        this.name = name;  
    }  
}
```
#### 调用方法
可以通过`Class`实例获取所有`Method`信息。`Class`类提供了以下几个方法来获取`Method`：
- `Method getMethod(name, Class...)`：获取某个`public`的`Method`（包括父类）
- `Method getDeclaredMethod(name, Class...)`：获取当前类的某个`Method`（不包括父类）
- `Method[] getMethods()`：获取所有`public`的`Method`（包括父类）
- `Method[] getDeclaredMethods()`：获取当前类的所有`Method`（不包括父类）
```java
public class Main {  
    public static void main(String[] args) throws  Exception {  
        Class stdClass = Student.class;  
        // 获取public方法getScore，参数为String:  
        System.out.println(stdClass.getMethod("getScore", String.class));  
        // 获取继承的public方法getName，无参数  
        System.out.println(stdClass.getMethod("getName"));  
        // 获取private方法getGrade，参数为int:  
        System.out.println(stdClass.getDeclaredMethod("getGrade", int.class));  
    }  
}  
class Person{  
    public String getName(){  
        return "Person";  
    }  
}  
class Student extends Person{  
    public int getScore(String type){  
        return 99;  
    }  
    private int getGrade(int year){  
        return 1;  
    }  
}
```
一个`Method`对象包含一个方法的所有信息：
- `getName()`：返回方法名称，例如：`"getScore"`；
- `getReturnType()`：返回方法返回值类型，也是一个Class实例，例如：`String.class`；
- `getParameterTypes()`：返回方法的参数类型，是一个Class数组，例如：`{String.class, int.class}`；
- `getModifiers()`：返回方法的修饰符，它是一个`int`，不同的bit表示不同的含义。
```java
public class Main {  
    public static void main(String[] args) throws  Exception {  
        //String对象  
        String s = "Hello World";  
        //获取String substring(int)方法，参数为int:  
        Method m = String.class.getMethod("substring", int.class);  
        //在s对象上调用该方法并获取结果  
        String r = (String) m.invoke(s,6);  
        System.out.println(r);  
    }  
}
```
**调用静态方法**
调用静态方法时，由于无需指定实例对象，所以`invoke`方法传入的第一个参数永远为`null`。我们以`Integer.parseInt(String)`为例：
```java
// 获取Integer.parseInt(String)方法，参数为String:  
Method m = Integer.class.getMethod("parseInt", String.class);  
// 调用该静态方法并获取结果:  
Integer n = (Integer) m.invoke(null,"123");  
// 打印调用结果:  
System.out.println(n);
```
**调用非public方法**
和Field类似，对于非public方法，我们虽然可以通过`Class.getDeclaredMethod()`获取该方法实例，但直接对其调用将得到一个`IllegalAccessException`。为了调用非public方法，我们通过`Method.setAccessible(true)`允许其调用：
#### 调用构造方法
我们通常使用`new`操作符创建新的实例
```java
Person p = new Person();
```
如果通过反射创建实例，可以调用Class提供的newInstance()方法
```java
Person p = Person.class.newInstance();
```
调用`Class.newInstance()`的局限是，它只能调用该类的public无参数构造方法。如果构造方法带有参数，或者不是public，就无法直接通过`Class.newInstance()`来调用。
为了调用任意的构造方法，Java的反射API提供了`Constructor`对象，它包含一个构造方法的所有信息，可以创建一个实例。`Constructor`对象和Method非常类似，不同之处仅在于它是一个构造方法，并且，调用结果总是返回实例：
```java
// 获取构造方法Integer(int):  
Constructor cons1 = Integer.class.getConstructor(int.class);  
// 调用构造方法:  
Integer n1 = (Integer) cons1.newInstance(123);  
System.out.println(n1);  
Constructor cons2 = Integer.class.getConstructor(String.class);  
Integer n2 = (Integer) cons2.newInstance("456");  
System.out.println(n2);
```
通过Class实例获取Constructor的方法如下：
- `getConstructor(Class...)`：获取某个`public`的`Constructor`；
- `getDeclaredConstructor(Class...)`：获取某个`Constructor`；
- `getConstructors()`：获取所有`public`的`Constructor`；
- `getDeclaredConstructors()`：获取所有`Constructor`。
#### 获取继承关系
有了`Class`实例，我们还可以获取它的父类的`Class`：
```java
Class i = Integer.class;  
Class n = i.getSuperclass();  
System.out.println(n);  
Class o = n.getSuperclass();  
System.out.println(o);  
System.out.println(o.getSuperclass());
```
根据输出
```
class java.lang.Number
class java.lang.Object
null
```
可以看出继承关系
```
Object
  |
Number
  |
Integer
```
除`Object`外，其他任何非`interface`的`Class`都必定存在一个父类类型。
**获取interface**
由于一个类可能实现一个或多个接口，通过`Class`我们就可以查询到实现的接口类型。例如，查询`Integer`实现的接口：
```java
public class Main {  
    public static void main(String[] args) throws  Exception {  
        Class s = Integer.class;  
        Class[] is = s.getInterfaces();  
        for(Class i : is){  
            System.out.println(i);  
        }  
    }  
}
```
运行上述代码可知，`Integer`实现的接口有：
```
interface java.lang.Comparable
interface java.lang.constant.Constable
interface java.lang.constant.ConstantDesc
```
要特别注意：`getInterfaces()`只返回当前类直接实现的接口类型，并不包括其父类实现的接口类型：
```java
public class Main {  
    public static void main(String[] args) throws  Exception {  
        Class s = Integer.class.getSuperclass();  
        Class[] is = s.getInterfaces();  
        for(Class i : is){  
            System.out.println(i);  
        }  
    }  
}
```
其父类实现的接口有
```
interface java.io.Serializable
```
此外，对所有`interface`的`Class`调用`getSuperclass()`返回的是`null`，获取接口的父接口要用`getInterfaces()`：
如果一个类没有实现任何`interface`，那么`getInterfaces()`返回空数组。
**继承关系**
当我们判断一个实例是否是某个类型时，正常情况下，使用`instanceof`操作符：
```java
Object n = Integer.valueOf(123);
boolean isDouble = n instanceof Double; // false
boolean isInteger = n instanceof Integer; // true
boolean isNumber = n instanceof Number; // true
boolean isSerializable = n instanceof java.io.Serializable; // true
```
#### 动态代理
Java标准库提供了一种动态代理（Dynamic Proxy）的机制：可以在运行期动态创建某个`interface`的实例。
所谓动态代理，是和静态相对应的。我们来看静态代码怎么写：
定义接口：
```java
public interface Hello {
    void morning(String name);
}
```
编写实现类：

```java
public class HelloWorld implements Hello {
    public void morning(String name) {
        System.out.println("Good morning, " + name);
    }
}
```

创建实例，转型为接口并调用：

```java
Hello hello = new HelloWorld();
hello.morning("Bob");
```
还有一种方式是动态代码，我们仍然先定义了接口`Hello`，但是我们并不去编写实现类，而是直接通过JDK提供的一个`Proxy.newProxyInstance()`创建了一个`Hello`接口对象。这种没有实现类但是在运行期动态创建了一个接口对象的方式，我们称为动态代码。JDK提供的动态创建接口对象的方式，就叫动态代理。
一个最简单的动态代理实现如下：
```java
public class Main {  
    public static void main(String[] args) throws  Exception {  
        //创建 InvocationHandler（调用处理器）  
        InvocationHandler handler = new InvocationHandler() {  
            @Override  
            //proxy 代理对象本身 method 方法对象 args 调用是传入的参数数组  
            public Object invoke(Object proxy,Method method ,Object[] args) throws Throwable {  
                System.out.println(method);  
                if(method.getName().equals("morning")){  
                    System.out.println("Good morning, " + args[0]);  
                }  
                return null;  
            }  
        };  
        Hello hello = (Hello) Proxy.newProxyInstance(  
                Hello.class.getClassLoader(),//类加载器  
                new Class[] {Hello.class},//要代理的接口列表  
                handler//调用处理器  
        );  
        hello.morning("Bob");//调用 morning 方法  
    }  
}  
//定义接口  
interface Hello{  
    void morning(String name);  
}
```
在运行期动态创建一个`interface`实例的方法如下：

1. 定义一个`InvocationHandler`实例，它负责实现接口的方法调用；
2. 通过`Proxy.newProxyInstance()`创建`interface`实例，它需要3个参数：
    1. 使用的`ClassLoader`，通常就是接口类的`ClassLoader`；
    2. 需要实现的接口数组，至少需要传入一个接口进去；
    3. 用来处理接口方法调用的`InvocationHandler`实例。
3. 将返回的`Object`强制转型为接口。
动态代理实际上是JVM在运行期动态创建class字节码并加载的过程，它并没有什么黑魔法，把上面的动态代理改写为静态实现类大概长这样：
```java
public class HelloDynamicProxy implements Hello {
    InvocationHandler handler;
    public HelloDynamicProxy(InvocationHandler handler) {
        this.handler = handler;
    }
    public void morning(String name) {
        handler.invoke(
           this,
           Hello.class.getMethod("morning", String.class),
           new Object[] { name }
        );
    }
}
```





## 参考文章
Java安全漫谈 - .反射篇
https://www.freebuf.com/articles/web/355185.html
https://www.cnblogs.com/lxfweb/p/16035764.html
https://pankas.top/2022/10/07/%E5%88%9D%E6%8E%A2java%E5%AE%89%E5%85%A8%E4%B9%8B%E5%8F%8D%E5%B0%84-1/
http://chen.oinsm.com/2024/04/24/Java%E5%AE%89%E5%85%A8-%E5%8F%8D%E5%B0%84%E7%AF%87/
https://zhuanlan.zhihu.com/p/456898438
https://liaoxuefeng.com/books/java/reflection/class/index.html